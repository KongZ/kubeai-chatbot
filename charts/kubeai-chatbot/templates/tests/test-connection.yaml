apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infra-ai.fullname" . }}-test-connection"
  labels:
    {{- include "infra-ai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infra-ai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
