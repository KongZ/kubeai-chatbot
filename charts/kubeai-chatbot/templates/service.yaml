apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-ai.fullname" . }}
  labels:
    {{- include "infra-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-ai.selectorLabels" . | nindent 4 }}
